source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"

# macOS still ships Ruby 2.6; pin the last compatible native dependency so the
# same site can be previewed without installing a second Ruby toolchain.
gem "ffi", "~> 1.15.5"
